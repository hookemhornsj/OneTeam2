<%= form_for(@required_skill) do |f| %>
  <% if @required_skill.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@required_skill.errors.count, "error") %> prohibited this required_skill from being saved:</h2>

      <ul>
      <% @required_skill.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :request_id %><br>
    <%= f.number_field :request_id %>
  </div>
  <div class="field">
    <%= f.label :skill_id %><br>
    <%= f.number_field :skill_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
